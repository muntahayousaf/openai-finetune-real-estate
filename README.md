# 🏠 Fine-Tuned Real Estate Chatbot

This project is a **fine-tuned OpenAI GPT model** deployed as a chatbot for **real estate data insights**.  
The model is trained on a global real estate dataset and deployed using **Streamlit** for an interactive web interface.

---

## 🚀 Project Overview

- **Model**: Fine-tuned GPT (gpt-4o-mini-2024-07-18)  
- **Dataset**: Global real estate dataset (used for fine-tuning)  
- **Deployment**: Streamlit web app  
- **Purpose**: Answer real estate-related queries intelligently based on the fine-tuned data  

The chatbot allows users to interact with a **custom GPT model** that understands real estate data, property trends, and related insights.

---

## 📂 Features

- Interactive chatbot interface using **Streamlit**  
- Fine-tuned model provides **domain-specific answers**  
- Maintains **conversation history** in the session  
- Lightweight and easy to deploy  

---

## ⚡ How It Works

1. **Fine-Tuning**  
   - The global real estate dataset is preprocessed and converted into JSONL format.  
   - A GPT-Mini / GPU-Mini model is fine-tuned using OpenAI’s fine-tuning API.  

2. **Deployment**  
   - Streamlit web app interacts with the fine-tuned model via OpenAI’s API.  
   - The app maintains session state to keep track of conversation history.  

3. **User Interaction**  
   - Users type queries about real estate.  
   - The chatbot responds with answers generated by the fine-tuned model.  

---
